class Data {

    // Включен ли режим редактирования
    static mode_edit: boolean = false;

    // Загружено ли поле классификатора
    static catalog_field_is_load: boolean = false;

    // Нет ли полей у узла
    static catalog_field_is_empty: boolean = true;

    // Загружен классификатор
    static catalog_is_load: boolean = false;

}


export {Data};
